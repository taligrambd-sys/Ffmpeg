name: Build Real Standard AAR without Gradle

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-so-libs:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        architecture: [arm64-v8a, armeabi-v7a]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Android NDK
      uses: android-actions/setup-android@v3

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential yasm nasm cmake git make zip

    - name: Download FFmpeg Source
      run: |
        git clone --depth 1 --branch release/6.0 https://github.com/FFmpeg/FFmpeg.git ffmpeg-src

    - name: Build Audio-Only FFmpeg so
      run: |
        cd ffmpeg-src
        NDK_PATH=$ANDROID_NDK_HOME
        TOOLCHAIN=$NDK_PATH/toolchains/llvm/prebuilt/linux-x86_64
        API=21
        
        if [ "${{ matrix.architecture }}" = "arm64-v8a" ]; then
          ARCH="aarch64"
          CPU="armv8-a"
          CROSS_PREFIX="$TOOLCHAIN/bin/aarch64-linux-android-"
          CC="$TOOLCHAIN/bin/aarch64-linux-android$API-clang"
          CXX="$TOOLCHAIN/bin/aarch64-linux-android$API-clang++"
        else
          ARCH="arm"
          CPU="armv7-a"
          CROSS_PREFIX="$TOOLCHAIN/bin/arm-linux-androideabi-"
          CC="$TOOLCHAIN/bin/armv7a-linux-androideabi$API-clang"
          CXX="$TOOLCHAIN/bin/armv7a-linux-androideabi$API-clang++"
        fi

        export LDFLAGS="-Wl,-z,max-page-size=16384"

        ./configure \
          --cross-prefix=$CROSS_PREFIX \
          --sysroot=$TOOLCHAIN/sysroot \
          --target-os=android \
          --arch=$ARCH \
          --cpu=$CPU \
          --enable-cross-compile \
          --enable-shared \
          --disable-static \
          --disable-doc \
          --disable-ffmpeg \
          --disable-ffplay \
          --disable-ffprobe \
          --disable-avdevice \
          --disable-swscale \
          --disable-postproc \
          --disable-avfilter \
          --disable-everything \
          --disable-vulkan \
          --disable-hwaccels \
          --disable-vdpau \
          --enable-avcodec \
          --enable-avformat \
          --enable-swresample \
          --enable-decoder=aac,mp3,flac,wav,opus \
          --enable-encoder=aac,mp3,flac,wav,opus \
          --enable-parser=aac,mpegaudio,opus \
          --enable-demuxer=aac,mp3,flac,wav,ogg,mov \
          --enable-muxer=adts,mp3,flac,wav,ogg,mp4 \
          --enable-protocol=file,http,https,tcp,udp \
          --cc=$CC \
          --cxx=$CXX \
          --enable-neon \
          --extra-ldflags="$LDFLAGS"

        make -j4
        
        mkdir -p ../output/${{ matrix.architecture }}
        cp libavcodec/libavcodec.so ../output/${{ matrix.architecture }}/
        cp libavformat/libavformat.so ../output/${{ matrix.architecture }}/
        cp libswresample/libswresample.so ../output/${{ matrix.architecture }}/
        cp libavutil/libavutil.so ../output/${{ matrix.architecture }}/

    - name: Upload Temporary SO Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: so-libs-${{ matrix.architecture }}
        path: output/${{ matrix.architecture }}/

  build-pure-aar:
    runs-on: ubuntu-latest
    needs: build-so-libs

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 17 (Java Compiler এর জন্য)
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Manual AAR Packaging
      run: |
        # ১. অফিশিয়াল AAR ফোল্ডার স্ট্রাকচার তৈরি
        mkdir -p aar-package/jni/arm64-v8a
        mkdir -p aar-package/jni/armeabi-v7a
        mkdir -p java-build/com/tonmoy/ffmpeg/audio
        
        # ২. AndroidManifest.xml তৈরি
        cat << 'EOF' > aar-package/AndroidManifest.xml
        <manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.tonmoy.ffmpeg.audio"></manifest>
        EOF

        # ৩. R.txt তৈরি (ব্ল্যাঙ্ক স্ট্যান্ডার্ড ফাইল)
        touch aar-package/R.txt

        # ৪. Native Library Loader জাভা ফাইল তৈরি
        cat << 'EOF' > java-build/com/tonmoy/ffmpeg/audio/FFmpegAudio.java
        package com.tonmoy.ffmpeg.audio;
        public class FFmpegAudio {
            static {
                System.loadLibrary("avutil");
                System.loadLibrary("swresample");
                System.loadLibrary("avcodec");
                System.loadLibrary("avformat");
            }
            public static String getInfo() { return "FFmpeg Audio Loaded Successfully"; }
        }
        EOF

        # ৫. জাভা কোডটি কম্পাইল করা
        javac java-build/com/tonmoy/ffmpeg/audio/FFmpegAudio.java
        
        # ৬. classes.jar ফাইল তৈরি করা এবং ওটাকে প্যাকেজে পাঠানো
        cd java-build
        jar cvf ../aar-package/classes.jar com/
        cd ..

    - name: Download arm64-v8a SO
      uses: actions/download-artifact@v4
      with:
        name: so-libs-arm64-v8a
        path: aar-package/jni/arm64-v8a

    - name: Download armeabi-v7a SO
      uses: actions/download-artifact@v4
      with:
        name: so-libs-armeabi-v7a
        path: aar-package/jni/armeabi-v7a

    # সব ফাইলকে একসাথে কম্প্রেস করে এক্সপিসিটলি .aar ফাইলে রূপান্তর
    - name: Generate Standard .AAR File
      run: |
        cd aar-package
        zip -r ../ffmpeg-audio-release.aar .

    - name: Upload Final AAR Artifact
      uses: actions/upload-artifact@v4
      with:
        name: ffmpeg-audio-aar
        path: ffmpeg-audio-release.aar
